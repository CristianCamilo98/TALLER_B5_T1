# Minimum runtime dependencies for David's official RealNVP Normalizing Flow
# (train_normalizing_flow.py, generate_normalized.py, src/normalizing_flow.py).
#
# All of these are already pinned in the repository root requirements.txt;
# this file exists to document David's actual import footprint explicitly.
# No deep-learning framework (TensorFlow, PyTorch) is required: the flow is
# implemented directly on top of numpy.
numpy>=1.26
pandas>=2.0
pyarrow>=14.0
matplotlib>=3.8
pytest>=8.0
